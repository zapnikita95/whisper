# Только Mac-клиент (whisper-client-mac.py, .app). Ставится в .venv в корне репозитория.
# См. packaging/mac/setup_mac_venv.sh
requests>=2.28.0
sounddevice>=0.4.6
numpy>=1.24.0
soundfile>=0.12.1
pynput>=1.8.1
pyperclip>=1.8.2
rumps>=0.4.0
# UNUserNotificationCenter из меню-бара (запрос разрешений); иначе «No module named UserNotifications»
pyobjc-framework-UserNotifications>=10.0
# Cmd+V fallback через CGEventPost, если osascript не вставляет (без pynput из фонового потока)
pyobjc-framework-Quartz>=10.0
