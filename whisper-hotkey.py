"""CLI: запуск из батников `start-whisper-hotkey.bat` (имя с дефисом). Логика — в whisper_hotkey_core."""
from whisper_hotkey_core import main

if __name__ == "__main__":
    raise SystemExit(main())
